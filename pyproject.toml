[project]
name = "first-gitscraper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "DiegoTorrealba", email = "DiegoTorrealba37@gmail.com" }
]
requires-python = ">=3.14.6"
dependencies = [
    "bs4>=0.0.2",
    "jupyterlab>=4.6.3",
    "requests>=2.34.2",
]

[project.scripts]
first-gitscraper = "first_gitscraper:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
